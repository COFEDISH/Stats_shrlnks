apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80   # порт внутри кластера
      targetPort: 5003  # порт вашего сервиса
